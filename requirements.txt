# =============================================
# Sistema de Controle de Saídas — requirements
# Testado com Python 3.11+
# =============================================

# Web framework
Flask>=3.0.0,<4.0

# ORM + migrations
Flask-SQLAlchemy>=3.1.1,<4.0
SQLAlchemy>=2.0.25,<3.0
Flask-Migrate>=4.0.0,<5.0
alembic>=1.13.0,<2.0

# Autenticação
Flask-Login>=0.6.3,<1.0
Flask-Bcrypt>=1.0.1,<2.0

# Variáveis de ambiente
python-dotenv>=1.0.1,<2.0

# Geração de PDF
reportlab>=4.2.0,<5.0

# Processamento de imagens
Pillow>=10.0.0,<13.0

# Banco MariaDB/MySQL (necessário apenas em produção; SQLite não requer extras)
PyMySQL>=1.1.0,<2.0
cryptography>=42.0.0,<44.0

# Agendador de tarefas (atualização automática de status de saídas)
APScheduler>=3.10.0,<4.0
